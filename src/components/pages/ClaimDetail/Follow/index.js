import { ClaimDetailContext } from "contexts";
import { useContext } from "preact/hooks";

export default function FollowClaim (props) {

	const {  } = useContext(ClaimDetailContext);

	return null;
}